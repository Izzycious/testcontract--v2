const inheritance = artifacts.require("inheritance");

contract("Inheritance", (accounts) => {
    it("should check if the inheritance", async () => {
        const inheritanceInstance = await inheritance.deployed();
        
    })
});